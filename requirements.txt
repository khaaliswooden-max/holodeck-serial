# holodeck-serial requirements
# Profiling, testing, and visualization tools only.
# The simulation kernel (src/core/, src/mvw/) uses Python stdlib exclusively.

# Testing
pytest>=7.0.0
pytest-benchmark>=4.0.0

# Profiling
psutil>=5.9.0          # Memory measurement for SCE-02
py-spy>=0.3.14         # CPU profiling (optional, CLI tool)

# Visualization (figures for paper)
matplotlib>=3.7.0      # SCE scaling law plot (Session 6)
numpy>=1.24.0          # Figure generation only — NOT used in simulation kernel

# Output
tabulate>=0.9.0        # Markdown table rendering in benchmark runner
